# URL gốc của FastAPI (Oracle / máy chủ backend), không có dấu / cuối.
# Bắt buộc khi deploy frontend lên Vercel; để trống khi dev (Vite proxy tới localhost:8000).
# VITE_API_BASE_URL=https://api.example.com
